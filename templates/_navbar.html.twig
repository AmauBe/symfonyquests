<nav class="navbar navbar-expand-lg bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand h1" href="/">WildSeries</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse h6" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active text-white" aria-current="page" href="{{path('app_index')}}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white" href="{{path('program_index')}}">Programs</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white" href="{{path('category_index')}}">Category</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white" href="/season/">Ajoute une saison</a>
				</li>
				<li class="nav-items">
					<a class="nav-link text-white" href="/actor/">Liste des Acteurs</a>
				</li>
				<li class="nav-items">
					<a class="nav-link text-white" href="{{path('app_episode_index')}}">Liste des episodes</a>
				</li>
			</ul>

			{% if app.user %}

				{{ app.user.email }}

				<a class='text-white' href="{{ path('app_logout') }}">
					Se déconnecter</a>

			{% else %}

				<a class='text-white' href="{{ path('app_login') }}">
					Se connecter</a>

			{% endif %}
		</div>
	</div>
</nav>
